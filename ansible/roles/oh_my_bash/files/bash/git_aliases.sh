#!/usr/bin/env bash
# shellcheck shell=bash

alias yolo="git push origin master --force --no-verify"
alias g='git'
alias gs='git status'
alias gcane='git commit --amend --no-edit'
alias gc="git checkout"
# (rest of attachment-provided aliases kept as-is)
